---
- hosts: localhost
  tasks:
    - name: Configure Installers
      import_tasks: ansible/main/configure-installers.yml

    - name: Run Installers
      import_tasks: ansible/main/run-installers.yml

    - name: Configure Environment
      import_tasks: ansible/main/configure-environment.yml
    
